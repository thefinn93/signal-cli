image: java:8-jdk
build:
  stage: build
  cache:
    paths:
      - /root/.gradle
  script:
    - ./gradlew build
    - ./gradlew installDist
    - cp -r build/install/signal-cli signal-cli/
  artifacts:
    paths:
      - signal-cli
    expire_in: 1 week
